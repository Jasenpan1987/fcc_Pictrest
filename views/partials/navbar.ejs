<nav role="navigation" class="navbar navbar-default">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a href="#" class="navbar-brand">Pic Trest</a>
    </div>
    <!-- Collection of nav links, forms, and other content for toggling -->
    <div id="navbarCollapse" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            <li class="<%if(currentpage=='all'){%>active<%}%>"><a href="/">All</a></li>
            <li class="<%if(currentpage=='mypic'){%>active<%}%>"><a href="/users">My Pics</a></li>
            <li class="dropdown">
                <a data-toggle="dropdown" class="dropdown-toggle" href="#">Add a Pic <b class="caret"></b></a>
                <ul role="menu" class="dropdown-menu" style="padding: 5% 5% 5% 5%">
                    <li>
                        <form action="/pic/newpic" method="post">
                            <h5>Add a new pic</h5>

                            <input type="text" placeholder="Url of your image"
                                   id="imgurl"
                                   name="imgurl"
                                   class="form-control"
                                   style="margin-top: 5%"
                                   required=""
                            >
                            <input type="text" placeholder="Title of your image"
                                   id="imgtitle"
                                   name="imgtitle"
                                   class="form-control"
                                   style="margin-top: 5%"
                                   required=""
                            >
                            <button type="submit"
                                    class="btn btn-primary btn-block"
                                    style="margin-top: 5%"
                            >Submit</button>
                        </form>
                    </li>
                </ul>
            </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li>
                <%if(!user){%>
                <a class="btn btn-block btn-social btn-twitter" href="/auth/twitter">
                    <span class="fa fa-twitter"></span>
                    Sign in with Twitter
                </a>
                <%}else{%>
                <a href="/auth/logout" class="btn btn-danger">Logout</a>
                <%}%>
            </li>
        </ul>
    </div>
</nav>